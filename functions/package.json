{
  "name": "civ-cc-cloud-functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "deploy": "tsc -p tsconfig.json && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^4.1.2",
    "firebase-functions": "^0.5"
  },
  "private": true
}
